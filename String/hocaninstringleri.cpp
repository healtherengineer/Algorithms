#include<stdio.h>
int main()
{
	// yazidaki boþluk sayýsýný bulan program ; 
	/*
	
	char metin[50]="Ege Barisan Adana Cukurova";
	int c=0;
	for(int i=0;i<50;i++)
	{
		if(metin[i]==' ')
		{
			c=c+1;
		}
	}
	printf("girilen '%s' karakter dizisinde %d kadar bosluk karakteri vardýr.",metin,c);
// boyut bulma ;
// null karakterine kadar göndercez
printf("\n\n");

int a=0;
for(int i=0;i<50;i++)
	{
		if(metin[i]=='\0')
		{
			i=50;
		}
		else
		{
			a=a+1;// boyutumuz
		}
	}

printf("girilen '%s' karakter dizisinde %d kadar  karakter vardýr.\n",metin,a);
// r den k ya kadar kopyalama.
int r,k,d;
printf("r degerini gir : "); scanf("%d",&r);
printf("k degerini gir : "); scanf("%d",&k);
d=k-r; // silinecek alanýmýz 
char kopya[d];
int t=0;
for(int j=r;j<=k;j++)
{
	kopya[t]=metin[j];
	t=t+1;
	}	
	
	printf("kopyalanan metin = %s",kopya);

	//r den k ya kadar silme o zaman üstüne yazcaz.
		
	char metin[50]="Ege Barisan Adana Cukurova";
	int c=0,r,k;
for(int i=0;i<50;i++)
{
	if(metin[i]!='\0')
	{
		c=c+1;
	}
}
	printf("metnin boyutu = %d",c);
	printf("r gir : "); scanf("%d",&r);
		printf("k gir : "); scanf("%d",&k);
	int d=r-k;
	for(int i=1;i<d;i++)
	{
		for(int j=k;j<=26-i+1;i++)
		{
			metin[j]=metin[j+1];
		}
	}
	
for(int j=0;j<26-d;j++)
{
	printf("%c\n",metin[j]);
}
	*/	
	
	
	

	
	
	
	
	
}
